<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.foldbridge"
    android:sharedUserId="android.uid.system">

    <!-- System-level permissions (must be signed with platform key) -->
    <uses-permission android:name="android.permission.DEVICE_STATE_MANAGER" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Recommended feature declaration -->
    <uses-feature android:name="android.hardware.sensor.hinge_angle" android:required="false" />

    <application
        android:label="FoldBridge"
        android:icon="@android:drawable/ic_menu_compass"
        android:hasCode="true">

        <service
            android:name=".FoldStateBridgeService"
            android:permission="android.permission.BIND_DEVICE_STATE_MANAGER"
            android:foregroundServiceType="specialUse"
            android:enabled="true"
            android:exported="false" />

        <receiver android:name=".FoldStateBridgeService$BootReceiver"
                  android:enabled="true"
                  android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>

